# Legacy supporting analyses

These files preserve exploratory supporting analyses. Do not use them as standard workflows.

- `scan_spectral.py`: Original `K` and Gabor-phase connectivity-spectrum scan. Replaced by `scripts/analyses/run_spectral.py` and `src/analysis/spectral.py`. The new Gaussian operators match within `2.4e-7`, and the Gabor patterns match exactly for the `N=15` check.
